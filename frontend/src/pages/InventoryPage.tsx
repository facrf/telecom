import { FormEvent, useEffect, useState } from 'react'
import { api, items, json } from '../api'
import { Address, Attachment, Category, Device, PortResult, PortScan, Project, Tag } from '../types'
import { ConfirmButton, Empty, Field, Modal, Notice, PageHeader, Panel, Status } from '../components'

const emptyDevice:Device={id:'',projectId:'',name:'',description:'',categoryId:'other',manufacturer:'',model:'',serialNumber:'',hostname:'',vlan:'',location:'',room:'',rack:'',rackPosition:'',operatingSystem:'',firmware:'',adminUrl:'',status:'unknown',notes:''}
export default function InventoryPage(){const[projects,setProjects]=useState<Project[]>([]);const[categories,setCategories]=useState<Category[]>([]);const[devices,setDevices]=useState<Device[]>([]);const[project,setProject]=useState('');const[query,setQuery]=useState('');const[selected,setSelected]=useState<Device>();const[form,setForm]=useState<Device>();const[addresses,setAddresses]=useState<Address[]>([]);const[tags,setTags]=useState<Tag[]>([]);const[assigned,setAssigned]=useState<Tag[]>([]);const[attachments,setAttachments]=useState<Attachment[]>([]);const[portScans,setPortScans]=useState<PortScan[]>([]);const[ports,setPorts]=useState<PortResult[]>([]);const[message,setMessage]=useState('');const loadDevices=async()=>setDevices(items(await api<{items:Device[]}>(`/devices?project_id=${project}&q=${encodeURIComponent(query)}`)));const loadDetail=async(device:Device)=>{setSelected(device);const[addressValue,tagValue,assignedValue,attachmentValue,scanValue]=await Promise.all([api<{items:Address[]}>(`/devices/${device.id}/addresses`),api<{items:Tag[]}>('/tags'),api<{items:Tag[]}>(`/tags/device/${device.id}`),api<{items:Attachment[]}>(`/attachments/device/${device.id}`),api<{items:PortScan[]}>(`/port-scans?device_id=${device.id}`)]);setAddresses(items(addressValue));setTags(items(tagValue));setAssigned(items(assignedValue));setAttachments(items(attachmentValue));setPortScans(items(scanValue));setPorts([])};useEffect(()=>{void Promise.all([api<{items:Project[]}>('/projects').then(v=>setProjects(items(v))),api<{items:Category[]}>('/device-categories').then(v=>setCategories(items(v))),loadDevices()])},[project]);const saveDevice=async(event:FormEvent)=>{event.preventDefault();if(!form)return;const saved=form.id?await api<Device>(`/devices/${form.id}`,json('PUT',form)):await api<Device>('/devices',json('POST',form));setForm(undefined);setMessage('Equipamento salvo.');await loadDevices();await loadDetail(saved)};const removeDevice=async()=>{if(!selected)return;await api(`/devices/${selected.id}`,{method:'DELETE'});setSelected(undefined);setMessage('Equipamento removido.');await loadDevices()};const addAddress=async(event:FormEvent<HTMLFormElement>)=>{event.preventDefault();if(!selected)return;const data=Object.fromEntries(new FormData(event.currentTarget));await api(`/devices/${selected.id}/addresses`,json('POST',{...data,primary:data.primary==='on'}));event.currentTarget.reset();await loadDetail(selected)};const upload=async(event:FormEvent<HTMLFormElement>)=>{event.preventDefault();if(!selected)return;const data=new FormData(event.currentTarget);await api(`/attachments/device/${selected.id}`,{method:'POST',body:data});event.currentTarget.reset();await loadDetail(selected)};const startPortScan=async(event:FormEvent<HTMLFormElement>)=>{event.preventDefault();if(!selected)return;const data=Object.fromEntries(new FormData(event.currentTarget));await api(`/devices/${selected.id}/port-scans`,json('POST',data));setMessage('Scan de portas iniciado.');setTimeout(()=>void loadDetail(selected),1200)};const assignedIDs=new Set(assigned.map(tag=>tag.id));return <><PageHeader eyebrow="INVENTÁRIO" title="Equipamentos" description="Cadastro permanente e diagnóstico por equipamento." actions={<button className="primary" onClick={()=>setForm({...emptyDevice,projectId:project})}>+ Novo equipamento</button>}/><Notice message={message} onClose={()=>setMessage('')}/><div className="toolbar"><select value={project} onChange={e=>setProject(e.target.value)}><option value="">Todos os projetos</option>{projects.map(value=><option key={value.id} value={value.id}>{value.name}</option>)}</select><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Nome, hostname, fabricante ou modelo"/><button onClick={()=>void loadDevices()}>Pesquisar</button></div><div className="split inventory-split"><Panel title={`Equipamentos (${devices.length})`} className="list-panel">{devices.length===0?<Empty>Nenhum equipamento encontrado.</Empty>:<div className="select-list">{devices.map(device=><button key={device.id} className={selected?.id===device.id?'selected':''} onClick={()=>void loadDetail(device)}><div><b>{device.name}</b><Status value={device.status}/></div><span>{device.hostname||device.manufacturer||'Sem identificação'}</span></button>)}</div>}</Panel><Panel title={selected?.name??'Detalhes'} actions={selected&&<><button onClick={()=>setForm({...selected})}>Editar</button><ConfirmButton message="Excluir este equipamento?" onConfirm={()=>void removeDevice()}>Excluir</ConfirmButton></>}>{!selected?<Empty>Selecione um equipamento.</Empty>:<div className="detail-stack"><dl className="details cols"><dt>Categoria</dt><dd>{categories.find(value=>value.id===selected.categoryId)?.name}</dd><dt>Fabricante / modelo</dt><dd>{[selected.manufacturer,selected.model].filter(Boolean).join(' ')||'—'}</dd><dt>Hostname</dt><dd>{selected.hostname||'—'}</dd><dt>Local</dt><dd>{[selected.location,selected.room,selected.rack].filter(Boolean).join(' · ')||'—'}</dd></dl><section><div className="section-heading"><h3>Endereços</h3></div>{addresses.map(address=><div className="inline-item" key={address.id}><code>{address.address}</code><span>{address.type} {address.interface}</span><ConfirmButton message="Remover endereço?" onConfirm={()=>void api(`/devices/${selected.id}/addresses/${address.id}`,{method:'DELETE'}).then(()=>loadDetail(selected))}>×</ConfirmButton></div>)}<form className="inline-form" onSubmit={addAddress}><select name="type"><option value="ipv4">IPv4</option><option value="ipv6">IPv6</option><option value="mac">MAC</option></select><input name="address" required placeholder="Endereço"/><input name="interface" placeholder="Interface"/><label><input name="primary" type="checkbox"/> Primário</label><button>Adicionar</button></form></section><section><div className="section-heading"><h3>Tags</h3><button onClick={async()=>{const name=prompt('Nome da nova tag');if(!name)return;await api('/tags',json('POST',{name,color:'#3b82f6'}));await loadDetail(selected)}}>+ Criar tag</button></div><div className="tag-list">{tags.map(tag=><button key={tag.id} className={assignedIDs.has(tag.id)?'tag assigned':'tag'} style={{borderColor:tag.color}} onClick={async()=>{await api(`/tags/${tag.id}/assign/device/${selected.id}`,{method:assignedIDs.has(tag.id)?'DELETE':'POST'});await loadDetail(selected)}}>{assignedIDs.has(tag.id)?'✓ ':''}{tag.name}</button>)}</div></section><section><div className="section-heading"><h3>Anexos</h3></div>{attachments.map(file=><div className="inline-item" key={file.id}><a href={`/api/v1/attachments/${file.id}/download`}>{file.filename}</a><span>{Math.ceil(file.size/1024)} KB</span><ConfirmButton message="Excluir anexo?" onConfirm={()=>void api(`/attachments/${file.id}`,{method:'DELETE'}).then(()=>loadDetail(selected))}>×</ConfirmButton></div>)}<form className="inline-form" onSubmit={upload}><input type="file" name="file" required/><input name="description" placeholder="Descrição"/><button>Enviar</button></form></section><section><div className="section-heading"><h3>Scan de portas</h3></div><form className="inline-form" onSubmit={startPortScan}><select name="mode"><option value="quick">Rápido</option><option value="standard">Padrão</option><option value="custom">Personalizado</option></select><input name="ports" placeholder="80,443,554 ou 1-1024"/><button>Iniciar</button></form>{portScans.map(scan=><button className="scan-row" key={scan.id} onClick={()=>void api<{items:PortResult[]}>(`/port-scans/${scan.id}/ports`).then(value=>setPorts(items(value)))}><span>{scan.mode}</span><Status value={scan.status}/><small>{scan.startedAt}</small></button>)}{ports.length>0&&<table><thead><tr><th>Porta</th><th>Serviço</th><th>Produto</th><th>Banner</th></tr></thead><tbody>{ports.map(port=><tr key={port.port}><td><code>{port.port}/tcp</code></td><td>{port.service}</td><td>{port.product} {port.version}</td><td className="truncate">{port.banner||'—'}</td></tr>)}</tbody></table>}</section></div>}</Panel></div>{form&&<Modal title={form.id?'Editar equipamento':'Novo equipamento'} onClose={()=>setForm(undefined)} wide><form onSubmit={saveDevice} className="form-grid"><Field label="Projeto *"><select required value={form.projectId} disabled={Boolean(form.id)} onChange={e=>setForm({...form,projectId:e.target.value})}><option value="">Selecione</option>{projects.map(value=><option key={value.id} value={value.id}>{value.name}</option>)}</select></Field><Field label="Nome *"><input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></Field><Field label="Categoria"><select value={form.categoryId} onChange={e=>setForm({...form,categoryId:e.target.value})}>{categories.map(value=><option key={value.id} value={value.id}>{value.name}</option>)}</select></Field><Field label="Status"><select value={form.status} onChange={e=>setForm({...form,status:e.target.value})}><option value="unknown">Desconhecido</option><option value="online">Online</option><option value="offline">Offline</option><option value="new">Novo</option><option value="changed">Alterado</option></select></Field>{(['manufacturer','model','serialNumber','hostname','vlan','location','room','rack','rackPosition','operatingSystem','firmware','adminUrl'] as (keyof Device)[]).map(key=><Field label={key} key={key}><input value={String(form[key]??'')} onChange={e=>setForm({...form,[key]:e.target.value})}/></Field>)}<Field label="Observações"><textarea value={form.notes} onChange={e=>setForm({...form,notes:e.target.value})}/></Field><div className="form-actions"><button type="button" onClick={()=>setForm(undefined)}>Cancelar</button><button className="primary">Salvar</button></div></form></Modal>}</>}
